{
  "rules": {
    "*/*": {
      "endpoint": "{service}.{region}.owlhub.io"
    },
    "*/iam": "globalSSL",
    "*/sts": "globalSSL",
    "*/owlverify": "globalSSL"
  },
  "patterns": {
    "globalSSL": {
      "endpoint": "https://{service}.owlhub.io",
      "globalEndpoint": true
    }
  }
}
